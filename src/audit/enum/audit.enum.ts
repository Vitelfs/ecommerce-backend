

export enum AuditType {
    USER = "USER",
    PRODUCT = "PRODUCT",
    CLIENT = "CLIENT"
}

export enum AuditAction {
    CREATE = "CREATE",
    UPDATE = "UPDATE",
    DELETE = "DELETE",
    LOGIN = "LOGIN"
}